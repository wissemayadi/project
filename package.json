{
  "name": "project4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently  \"    cd backend && nodemon server     \"      \"  cd client && npm start     \"   "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@szhsin/react-menu": "^1.9.0",
    "concurrently": "^6.0.0"
  }
}
